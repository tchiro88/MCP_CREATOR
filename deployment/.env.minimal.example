# Minimal MCP Setup - Environment Variables
# Copy to .env and fill in your values

# ============================================================================
# Cloudflare Tunnel (REQUIRED)
# ============================================================================
# Get this token by running: cloudflared tunnel create my-mcp-tunnel
CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here

# ============================================================================
# GitHub Access (REQUIRED for GitHub MCP)
# ============================================================================
# Create at: https://github.com/settings/tokens
# Scopes needed: repo, read:user
GITHUB_TOKEN=ghp_your_github_token_here

# ============================================================================
# Google Services (Gmail, Drive, Calendar)
# ============================================================================
# NOTE: Google MCP uses OAuth flow, not direct credentials
# You need to:
# 1. Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
# 2. Download credentials.json
# 3. Run the server locally once to authenticate and generate token.json
# 4. Copy both files to deployment/credentials/google/ directory
# See mcp/google/README.md for detailed instructions

# ============================================================================
# Todoist (REQUIRED for Todoist MCP)
# ============================================================================
# Get your API token from: https://todoist.com/prefs/integrations
TODOIST_API_TOKEN=your-todoist-api-token-here
