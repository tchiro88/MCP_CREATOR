# Minimal Cloudflare Tunnel Configuration
# Only routes to essential MCP servers

tunnel: YOUR-TUNNEL-ID-HERE
credentials-file: /etc/cloudflared/credentials.json

ingress:
  # GitHub MCP Server
  - hostname: github.yourdomain.com
    service: http://mcp-github:3000

  # Filesystem MCP Server
  - hostname: files.yourdomain.com
    service: http://mcp-filesystem:3000

  # Gmail MCP (when you add it)
  # - hostname: gmail.yourdomain.com
  #   service: http://mcp-gmail:3000

  # Google Drive MCP (when you add it)
  # - hostname: drive.yourdomain.com
  #   service: http://mcp-gdrive:3000

  # Catch-all
  - service: http_status:404
